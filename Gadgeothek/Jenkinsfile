pipeline {
    agent {
        node {
            label 'jenkins-slave7.yvu.ch'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'echo clean build'
                sh '(cd Gadgeothek; ./gradlew clean build)'
            }
            post {
                always {
                    junit 'Gadgeothek/app/build/test-results/testDemoConfigDebugUnitTest/*.xml'
                }
            }
        }
        stage('End2End Tests') {
            steps {
                sh 'echo End2End Tests'
                sh '(cd Gadgeothek; ./gradlew connectedAndroidTest)'
            }
        }
        stage('Publish') {
            steps {
                sh 'echo Publish'
            }
        }
    }
}